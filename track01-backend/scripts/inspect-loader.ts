// import { loadDocuments } from "../src/ingestion/document-loader";

// let count = 0;

// for await (const doc of loadDocuments()) {
//   console.log({
//     source: doc.source,
//     sourceId: doc.sourceId,
//     // uuid: doc.datasetDocUuid,
//     title: doc.title,
//     contentLength: doc.content.length,
//   });

//   count++;

//   if (count >= 10) {
//     break;
//   }
// }

// console.log(`Loaded ${count} documents`);